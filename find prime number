#include <iostream>

using namespace std;

int main()
{
   int n,flag=0;
   cin>>n;
   if(n<2)
   {
       cout<<"Not Prime";
   }
   else
   {
       for(int i=2;i<n-1;i++)
       {
           if(n%i==0)
           flag=1;
       }
       if(flag==1)
       cout<<"not prime";
       if(flag==0)
       cout<<"Prime";
   }

    return 0;
}
//O(n)
